spring:
  datasource:
    username:root
    password:123456
    url:jdbc:mysql://localhost:3306/mybank?characterEncoding=latin1&useConfigs=maxPerformance
    driver-class-name:com.mysql.jdbc.Driver
  jpa:
      show-sql: true
      hibernate:
      ddl-auto: update
      properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect